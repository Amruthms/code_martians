╔════════════════════════════════════════════════════════════════════════════╗
║                  EC2 DEPLOYMENT STEPS - FOLLOW THIS GUIDE                  ║
║                         IP: 54.206.93.52                                   ║
╚════════════════════════════════════════════════════════════════════════════╝

✅ STEP 1: CONFIGURE EC2 SECURITY GROUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before deploying, open these ports in your EC2 Security Group:

1. Go to AWS Console → EC2 → Instances
2. Select your instance (54.206.93.52)
3. Click "Security" tab
4. Click the security group name
5. Click "Edit inbound rules"
6. Add these rules:

   ┌──────┬──────────┬─────────────┬──────────────────────┐
   │ Port │ Protocol │ Source      │ Description          │
   ├──────┼──────────┼─────────────┼──────────────────────┤
   │ 22   │ TCP      │ Your IP     │ SSH Access           │
   │ 80   │ TCP      │ 0.0.0.0/0   │ Frontend (HTTP)      │
   │ 8000 │ TCP      │ 0.0.0.0/0   │ Backend API          │
   └──────┴──────────┴─────────────┴──────────────────────┘

7. Click "Save rules"


✅ STEP 2: CONNECT TO YOUR EC2 INSTANCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open your terminal and SSH into EC2:

# For Ubuntu instances:
ssh -i "your-key.pem" ubuntu@54.206.93.52

# For Amazon Linux instances:
ssh -i "your-key.pem" ec2-user@54.206.93.52

# If permission error on key file:
chmod 400 your-key.pem


✅ STEP 3: CLONE THE REPOSITORY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Once connected to EC2, run:

git clone https://github.com/Amruthms/code_martians.git
cd code_martians


✅ STEP 4: CHECKOUT MASTER BRANCH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Switch to the optimized master branch:

git checkout master
git branch
# Should show: * master


✅ STEP 5: RUN AUTOMATED DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run the automated setup script (this will take 10-15 minutes):

chmod +x ec2-setup.sh
./ec2-setup.sh

This script will:
  ✓ Install Docker and Docker Compose
  ✓ Build all Docker images (backend, frontend, yolov8-model)
  ✓ Start all services
  ✓ Display the status

🔄 Wait for the script to complete...


✅ STEP 6: VERIFY DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After deployment completes, verify everything is running:

# Check service status
docker-compose ps

Expected output:
┌────────────────────┬─────────────────┬──────────────────┐
│ Name               │ State           │ Ports            │
├────────────────────┼─────────────────┼──────────────────┤
│ safety-backend     │ Up (healthy)    │ 0.0.0.0:8000->.. │
│ safety-frontend    │ Up (healthy)    │ 0.0.0.0:80->80   │
│ yolov8-model       │ Up              │ -                │
└────────────────────┴─────────────────┴──────────────────┘

# Test backend API
curl http://localhost:8000/stats

# Test frontend
curl http://localhost:80

# View logs (Ctrl+C to exit)
docker-compose logs -f


✅ STEP 7: ACCESS YOUR APPLICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open your browser and navigate to:

🌐 Frontend Dashboard:    http://54.206.93.52:80
🔧 Backend API:           http://54.206.93.52:8000
📚 API Documentation:     http://54.206.93.52:8000/docs
📖 Alternative Docs:      http://54.206.93.52:8000/redoc


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                           USEFUL COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

View logs:
  docker-compose logs -f                # All services
  docker-compose logs -f backend        # Backend only
  docker-compose logs -f frontend       # Frontend only

Check status:
  docker-compose ps                     # Service status
  docker stats                          # Resource usage

Restart services:
  docker-compose restart                # Restart all
  docker-compose restart backend        # Restart backend only

Stop services:
  docker-compose down                   # Stop all services
  docker-compose down -v                # Stop and remove volumes

Update application:
  git pull origin master                # Pull latest changes
  docker-compose down                   # Stop services
  docker-compose build --no-cache       # Rebuild images
  docker-compose up -d                  # Start services

Monitor resources:
  docker stats                          # Docker resource usage
  free -h                               # Memory usage
  df -h                                 # Disk usage


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                            TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ Problem: Services won't start
Solution:
  docker-compose logs                   # Check error logs
  docker-compose down -v                # Stop and remove
  docker-compose up -d                  # Start again

❌ Problem: Out of memory
Solution:
  # Add 4GB swap space
  sudo fallocate -l 4G /swapfile
  sudo chmod 600 /swapfile
  sudo mkswap /swapfile
  sudo swapon /swapfile
  echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

❌ Problem: Port already in use
Solution:
  sudo lsof -i :80                      # Check port 80
  sudo lsof -i :8000                    # Check port 8000
  sudo kill -9 <PID>                    # Kill the process

❌ Problem: Can't access from browser
Solution:
  1. Check security group has ports 80 and 8000 open
  2. Check services are running: docker-compose ps
  3. Check logs: docker-compose logs
  4. Try from EC2: curl http://localhost:80

❌ Problem: Docker group permission denied
Solution:
  sudo usermod -aG docker $USER
  newgrp docker
  # Or logout and login again


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                          ADDITIONAL RESOURCES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 Detailed Deployment Guide:  cat EC2_DEPLOYMENT.md
📄 Docker Review:              cat DOCKER_REVIEW.md
📄 Quick Deployment Steps:     cat DEPLOY_TO_EC2.md
📄 Master Branch README:       cat README_MASTER.md

🔍 Verify configuration:       ./verify-docker.sh


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                        DEPLOYMENT CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before declaring success, verify:

☐ Security group has ports 22, 80, 8000 open
☐ Can SSH to EC2 instance
☐ Repository cloned and on master branch
☐ Docker and Docker Compose installed
☐ All Docker images built successfully
☐ All 3 services running (docker-compose ps)
☐ Backend health check passes (curl http://localhost:8000/stats)
☐ Frontend accessible (curl http://localhost:80)
☐ Can access frontend in browser (http://54.206.93.52:80)
☐ Can access backend in browser (http://54.206.93.52:8000/docs)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                          🎉 READY TO DEPLOY! 🎉
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

All changes have been committed and pushed to the master branch.
Follow the steps above to deploy on your EC2 instance.

Good luck! 🚀
